{
  "name": "database",
  "version": "0.0.0",
  "dependencies": {
    "@prisma/client": "^5.3.1"
  },
  "devDependencies": {
    "@types/node": "^17.0.12",
    "prisma": "^5.3.1"
  },
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push --skip-generate",
    "db:migrate": "prisma migrate dev"
  },
  "main": "./index.ts",
  "types": "./index.ts"
}
